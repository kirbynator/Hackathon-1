<h1><%= @trip.name %></h1>
<%= link_to "Edit", edit_trip_path %>
<%= link_to "Delete", trips_path, method: :delete%>
<%= link_to "Back", trips_path %>

<h2>Locations</h2>
<%= link_to "Add Locations", new_trip_location_path(@trip) %>
<hr />
<ul>
    <% @trip.locations.each do |location| %>
    <li>
   
    <%= link_to location.name, trip_location_path(location.trip_id, location) %>
    
    </li>
    <% end %>

</ul>

