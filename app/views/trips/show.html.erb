<h1><%= @trip.name %></h1>
<%= link_to "Edit", edit_user_trip_path %>
<%= link_to "Back", user_trips_path %>

<h2>Locations</h2>
<%= link_to, "Add Locations", new_trip_locations_path %>
<hr />
<ul>
    <% @trip.location.each do |location| %>
    <li>
    <%= link_to, location.name, locations_path %>
    </li>

</ul>

